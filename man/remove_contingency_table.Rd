% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_contingency_table.R
\name{remove_contingency_table}
\alias{remove_contingency_table}
\title{Remove Contingency Table Columns}
\usage{
remove_contingency_table(tbl)
}
\arguments{
\item{tbl}{A data frame potentially containing the contingency table columns to be removed.}
}
\value{
A data frame with the contingency table columns removed if they were present.
}
\description{
Removes the contingency table columns (\code{a}, \code{b}, \code{c}, \code{d}, \code{n1}, \code{n2}, \code{N}) from the data frame.
This function is useful for cleaning up the data frame after computing keyness metrics
when the contingency table columns are no longer needed.
It safely removes only the columns that exist in the data frame.
}
\examples{
library(dplyr)

# Sample data with some contingency table columns
df <- data.frame(
  token = c("word1", "word2"),
  a = c(10, 5),
  n1 = c(100, 100)
)

# Remove contingency table columns
df_clean <- remove_contingency_table(df)

# View the result
print(df_clean)

}
